<header class="main-header">
	<div class="container header-inner">

		<div class="logo">
			<a href="{{ path('home') }}">🍷 Restaurant Manager</a>
		</div>

		<nav class="nav">
			<ul>
				<li><ahref="{{ path('dashboard') }}">Dashboard</a></li>
				<li><ahref="{{ path('commande_list') }}">Commandes</a></li>
				<li><ahref="{{ path('plat_list') }}">Plats</a></li>
				<li><ahref="{{ path('serveurs_list') }}">Serveurs</a></li>
			</ul>
		</nav>

		<div class="user-actions">
			{% if app.session.get('user') %}
				<span class="user-name">👤
					{{ app.session.get('user').prenom }}</span>
				<a class="btn-logout" href="{{ path('logout') }}">Déconnexion</a>
			{% else %}
				<a class="btn-login" href="{{ path('login') }}">Connexion</a>
			{% endif %}
		</div>

	</div>
</header>
